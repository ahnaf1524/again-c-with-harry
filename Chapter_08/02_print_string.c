#include <stdio.h>
int main()
{
    char str[] = "Phitron";
    // print string (manually) ==> Not need
    // for (int i = 0; i < 7; ++i)
    // {
    //     printf("%c", str[i]);
    // }

    printf("%s", str); // most used for string output using %s format spec
    // str is the first of element of character string

    return 0;
}
// Starts from : 7:39:04