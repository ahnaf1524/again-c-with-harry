#include <stdio.h>
int main()
{
    // Reserves space for 90 integer below
    int marks[90]; // we wanna to store 90 students marks
    marks[0] = 45;
    marks[1] = 95;
    printf("Marks 0 is %d Marks 1 is %d\n", marks[0], marks[1]);
    return 0;
}